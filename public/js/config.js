const config = {
    api: {
        baseUrl: 'https://wallet-server-13x5.onrender.com',
        endpoints: {
            transactions: '/api/transactions',
            categories: '/api/categories',
            accounts: '/api/accounts',
            budgets: '/api/budgets',
            reports: '/api/reports'
        }
    }
};

export default config; 